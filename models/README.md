# Model files

Place the exported YOLO NCNN model used by the robot in this directory as:

```text
models/best_ncnn_model/
```

The model itself was not included in the supplied project attachments used to build this repository package.
